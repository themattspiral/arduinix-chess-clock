{
  "name": "arduinix-chess-clock",
  "version": "0.2.0",
  "description": "An Ardunio Uno + Arduinix shield nixie tube chess clock/timer sketch, with Node.js mobile notification bridge (gets serial updates from Arduino and sends ntfy.sh messages)",
  "main": "notification-bridge.mjs",
  "scripts": {
    "dev": "node --env-file=.dev.env notification-bridge.mjs",
    "start": "node --env-file=.env notification-bridge.mjs"
  },
  "author": "Matt Ritter",
  "license": "MIT",
  "dependencies": {
    "serialport": "^13.0.0"
  }
}
